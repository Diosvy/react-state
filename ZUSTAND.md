Usa Zustand (store global):

Carrito de compras (lo leen el header, la página de producto y el checkout).

Usuario autenticado (lo lee casi toda la app).

Filtros de una lista que afectan a varios componentes hermanos.

Estado de un wizard multi-paso donde cada paso es un componente distinto.

Preferencias de UI (tema, sidebar abierto) que se comparten en muchas ramas.